# dynamic_sliding_window
Used when window size is not fixed.

Dynamic Sliding Window Template

This template handles problems where:
- Window size is variable
- Expand right pointer
- Shrink left pointer when condition breaks

Time Complexity: O(n)

Example Problem:
    Find the smallest subarray length with sum >= target.
    Returns length or 0 if not found.
